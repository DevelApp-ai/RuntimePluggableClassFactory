mode: ContinuousDeployment
next-version: 2.0.1
branches:
  master:
    regex: ^master$
    mode: ContinuousDeployment
    label: ''
    increment: Patch
    is-release-branch: true
  develop:
    regex: ^develop$
    mode: ContinuousDeployment
    label: alpha
    increment: Minor
    is-release-branch: false
  feature:
    regex: ^features?[/-]
    mode: ContinuousDeployment
    label: alpha
    increment: Minor
    is-release-branch: false
  hotfix:
    regex: ^hotfix(es)?[/-]
    mode: ContinuousDeployment
    label: beta
    increment: Patch
    is-release-branch: false
ignore:
  sha: []

